version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.2-browsers
    steps:
      - checkout

      - run:
          name: Which bundler?
          command: bundle -v

      - run:
          name: bundle install
          command: bundle check || bundle install --without production staging

      - run:
          name: Tests
          command: rake
