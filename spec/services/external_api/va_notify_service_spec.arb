# frozen_string_literal: true

describe ExternalApi::VANotifyService do
    before(:each) do
      stub_const("VANotifyService", Fakes::VANotifyService)
    end
  
    describe "#send_notification" do
      it "sends notifications" do
        result = VANotifyService.send_notification(
          
          )
  
        expect(result.error).to be_nil
        expect(result.data).to_not be_nil
      end
    end
  
    describe "#get_status" do
      it "returns the information for get_status" do
        result = VANotifyService.get_status()
  
        expect()
      end
    end
  
    describe "#create_callback" do
      it "returns the information for a callback" do
        result = VANotifyService.create_callback()
        expect().to be

      end
    end
  
    describe "#get_callback" do
      it "returns the information of the created callback" do
        result = VANotifyService.get_callback()
  
        expect(result.error).to be
      end
    end
  
    describe "#create_template" do
      it "it creates the template" do
        result = VANotifyService.create_template()
  
        expect(result.error).to be
      end
    end
    
    describe "#get_template" do
      it "returns the template" do
        result = VANotifyService.get_template()
  
        expect(result.error).to be_nil
        expect(result.all_ids_present?).to
      end
    end
  end