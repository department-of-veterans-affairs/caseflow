import { useState } from "react";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { select } from "@storybook/addon-knobs";

import { HearingTime } from "./HearingTime";
import { hearingTimeOptsWithZone } from "../../utils";

export const defaultProps = {};

<Meta
  title="Hearings/Components/Hearing Details/Virtual Hearings/HearingTime"
  component={HearingTime}
/>

# Hearing Time

### Default

<Preview>
  <Story name="Default">
    {() => {
      const [value, setValue] = useState("08:30");
      return (
        <div style={{ width: 500 }}>
          <HearingTime
            value={select("Time", hearingTimeOptsWithZone(), "08:30")}
            onChange={setValue}
          />
        </div>
      );
    }}
  </Story>
</Preview>

### Not Disabled

<Preview>
  <Story name="Not Disabled">
    {() => {
      const [value, setValue] = useState("");
      return (
        <div style={{ width: 500 }}>
          <HearingTime
            readOnly={false}
            value={select("Time", hearingTimeOptsWithZone(), "08:30")}
            onChange={setValue}
          />
        </div>
      );
    }}
  </Story>
</Preview>

### With Errors

<Preview>
  <Story name="With Errors">
    {() => {
      const [value, setValue] = useState("");
      return (
        <div style={{ width: 500 }}>
          <HearingTime
            errorMessage="Invalid time selected"
            value={select("Time", hearingTimeOptsWithZone(), "08:30")}
            onChange={setValue}
          />
        </div>
      );
    }}
  </Story>
</Preview>

<Props of={HearingTime} />
