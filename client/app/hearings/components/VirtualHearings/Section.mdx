import { useState } from "react";
import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { action } from "@storybook/addon-actions";
import { boolean, text } from "@storybook/addon-knobs";

import { VirtualHearingSection } from "./Section";

export const defaultProps = {};

<Meta
  title="Hearings/Components/Virtual Hearings/VirtualHearingSection"
  component={VirtualHearingSection}
/>

# Default

<Preview>
  <Story name="Normal">
    {() => {
      const [value, setValue] = useState("");
      return (
        <div style={{ width: 500 }}>
          <VirtualHearingSection
            hide={boolean("Hide", false, "knobs")}
            label={text("Label", "Example", "knobs")}
          >
            {text("Children", "Some words", "knobs")}
          </VirtualHearingSection>
        </div>
      );
    }}
  </Story>
</Preview>

### Hidden

<Preview>
  <Story name="Hidden">
    {() => {
      const [value, setValue] = useState("");
      return (
        <div style={{ width: 500 }}>
          <VirtualHearingSection
            hide={true}
            label={text("Label", "Example", "knobs")}
          >
            {text("Children", "Some words", "knobs")}
          </VirtualHearingSection>
        </div>
      );
    }}
  </Story>
</Preview>

<Props of={VirtualHearingSection} />
