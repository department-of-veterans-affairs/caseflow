import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { date, text, boolean } from '@storybook/addon-knobs';
import { addDecorator } from '@storybook/react';

import { ReadOnly } from './ReadOnly';

export const defaultProps = {};

<Meta
  title="Hearings/Components/Hearing Details/Read Only"
  component={ReadOnly}
/>

# Read Only

<Preview>
  <Story name="Normal">
    {() => (
      <ReadOnly text={text('Children', 'Some children', 'knobs')} label={text('Label', 'Example', 'knobs')}/>
    )}
  </Story>
</Preview>

<Props of={ReadOnly} />
