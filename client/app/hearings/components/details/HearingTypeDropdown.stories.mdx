import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { date, text, boolean, select } from "@storybook/addon-knobs";
import { addDecorator } from "@storybook/react";
import { action } from "@storybook/addon-actions";

import HearingTypeDropdown from "./HearingTypeDropdown";

export const defaultProps = {};

<Meta
  title="Hearings/Components/Hearing Details/Hearing Type Dropdown"
  component={HearingTypeDropdown}
/>

# Read Only

<Preview>
  <Story name="Normal">
    {() => (
      <HearingTypeDropdown
        convertHearing={(e) => action("convert")(e.target)}
        update={(e) => action("convert")(e.target)}
        openModal={(e) => action("convert")(e.target)}
        originalRequestType={select("Type", ["Video", "Central"], "Video", "knobs")}
        readOnly={boolean("Read-Only", false, "knobs")}
      />
    )}
  </Story>
</Preview>

<Props of={HearingTypeDropdown} />
