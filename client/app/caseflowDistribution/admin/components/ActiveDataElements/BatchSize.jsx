import React, { useState, useEffect } from 'react';
import { useSelector } from 'react-redux';

export const BatchSize = (props) => {
  return (
    <div>BatchSize</div>
  );
};

export default BatchSize;
