import React from 'react';

import { ScheduleHearingTaskAlert } from './ScheduleHearingTaskAlert';

export default {
  title: 'Queue/Substitute Appellant/Tasks/ScheduleHearingTaskAlert',
  component: ScheduleHearingTaskAlert,
};

const Template = (args) => <ScheduleHearingTaskAlert {...args} />;

export const Default = Template.bind({});
