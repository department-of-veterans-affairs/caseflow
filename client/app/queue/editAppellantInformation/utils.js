// Used to map form data from the form to the shape expected by the API
export const mapAppellantDataToApi = (appellant) => {
  // TODO: Map the incoming data to match the shape expected by the API
  return appellant;
}

// Use to map response data from the API to the shape expected by the UI
export const mapAppellantDataFromApi = (appellant) => {
  // TODO: Map the response data from the API to the shape expected by the UI
  return appellant
}