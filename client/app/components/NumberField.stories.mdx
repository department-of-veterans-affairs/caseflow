import { Meta, Story, ArgsTable, Canvas } from '@storybook/addon-docs/blocks';

import * as Stories from './NumberField.stories';

import NumberField from './NumberField';

<Meta
  title='Commons/Components/Form Fields/NumberField'
  component={NumberField}
/>

# NumberField 

NumberField is an editable text field where only integers are valid.

<Canvas>
  <Story story={Stories.Default} />
</Canvas>

## Args

<ArgsTable of={NumberField} />