<% content_for :page_title do certification_header("Feedback") end %>
<% content_for :new_window_title do %> Send Feedback | Caseflow <% end %>

<% content_for :full_page_content do %>
  <%= react_component("Feedback", props: {
    userDisplayName: current_user ? current_user.display_name : "",
    dropdownUrls: dropdown_urls,
    feedbackUrl: feedback_url,
    buildDate: build_date,
    dependenciesFaked: ApplicationController.dependencies_faked?,
  }) %>
<% end %>
