<% content_for :page_title do appeal.task_header end %>

<% content_for :full_page_content do %>
  <%= react_component("BaseContainer", props: {
    page: "DecisionReviewer",
    appealDocuments: [single_document.to_hash],
    annotations: single_document.annotations.map(&:to_hash),
    pdfWorker: asset_path('pdf.worker.js'),
    pdfLink: pdf_reader_appeal_document_path(id: single_document.id, time: Time.now.to_i)
  }) %>
<% end %>
