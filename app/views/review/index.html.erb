<% content_for :full_page_content do %>
  <%= react_component("BaseContainer", props: {
    page: "DecisionReviewer",
    appealDocuments: @documents,
    annotations: @appeal.saved_documents.map(&:annotations).flatten.map(&:to_hash),
    pdfWorker: asset_path('pdf.worker.js'),
    url: pdf_review_index_path
  }) %>
<% end %>
