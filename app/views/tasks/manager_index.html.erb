<%
  count = '0'
  count = Rails.cache.read('employee_count') if Rails.cache.read('employee_count')
%>
<%= react_component("BaseContainer", props: {
  page: "TasksManagerIndex",
  currentTasks: current_tasks.map(&:to_hash),
  completedTasks: completed_tasks.map(&:to_hash),
  toCompleteCount: @to_complete_count,
  completedCountToday: @completed_count_today,
  completedCountTotal: completed_count_total,
  employeeCount: count
}) %>
