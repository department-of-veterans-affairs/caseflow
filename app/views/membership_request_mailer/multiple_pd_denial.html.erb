<%= render "intro" %>

<%= content_for :content do %>
  <p>
    At this time, we have denied your request for access to <%= @requesting_org_name %> pages in Caseflow.
  </p>

  <p style="padding-top:20px">
    This does not impact access you had prior to this decision, or your pending requests for other VHA Pre-Docket offices listed below.
  </p>

  <p>
    <b> Your pending requests: </b>
  </p>

  <% @requested_groups.each do |requested_group| %>
    <p> <%= requested_group %> </p>
  <% end %>

  <p>
    Each Pre-docket office will send a separate decision email with their decision.
  </p>

  <p style="padding-top:20px">
    <b> You currently have access to the following groups’ pages, if any: </b>
  </p>

  <% @accessible_groups.each do |accessible_group| %>
    <p> <%= accessible_group %> </p>
  <% end %>

<% end %>

<%= render "help" %>
<%= render "requesting_org_signature" %>
