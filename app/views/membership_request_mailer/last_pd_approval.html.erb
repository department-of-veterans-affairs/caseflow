<!-- 12.5.2 -->
<%= render "intro" %>

<%= content_for :content do %>
  <p>
    We approved your request for Caseflow "VHA pre-docket organization granting access" pages, which also includes access to the VHA pages.

    You now have access to the following groups' pages:
    "VHA pre-docket organization granting access"
    "VHA General"
    "Any other groups"

    <%= render "help" %>
  </p>
<% end %>

<%= render "signature" %>
