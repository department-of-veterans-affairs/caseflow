<% content_for :page_title do page_title('Schedule') end %>

<% content_for :right_actions do %><%= print_link %><% end %>

<%= react_component("Hearings", props: {
  hearings: {
    veteran_law_judge: current_user.to_hash,
    dockets: current_user_dockets.map(&:to_hash)
  }
}) %>
