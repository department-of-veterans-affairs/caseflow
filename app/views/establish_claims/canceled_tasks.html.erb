<% content_for :new_window_title do %>Canceled EPs<% end %>

<% content_for :page_title do page_title('Canceled EPs') end %>

<%= react_component("BaseContainer", props: {
    canceledTasks: @canceled_tasks.map(&:to_hash),
    page: "CanceledTasksIndex"
}) %>
