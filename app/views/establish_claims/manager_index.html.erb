<%= react_component("ManageEstablishClaim", props: {
  toCompleteCount: to_complete_count,
  completedCountToday: @completed_count_today,
  employeeCount: team_quota.user_count,
  userQuotas: team_quota.assigned_quotas.map(&:to_hash),
}) %>
