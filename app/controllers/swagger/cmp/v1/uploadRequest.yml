type: object
title: Upload Request
description: Upload request containing the file binary and upload payload data
required:
  - payload
  - file
properties:
  payload:
    $ref: './payload.yml'
  file:
    type: string
    format: binary
